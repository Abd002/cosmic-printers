# cosmic-printers

Standalone printer backend/client crates extracted from COSMIC Settings Daemon printer work.

This repository contains:

- `printers-core`: shared printer types
- `printers-client`: client API
- `printers-server`: CUPS-backed printer server logic
